# AGENTS.md — Phrabit / Codex 向け常設ルール

このファイルは Codex（VSCode）が作業開始時に読むプロジェクト指示書。  
プロダクト定義は `docs/product-vision.md`、実装の現状・完了事項は `docs/handoff-codex.md` を参照。

---

## プロジェクト

- **名前:** Phrabit（フレービット）
- **種別:** Next.js 16 App Router + TypeScript + Tailwind v4
- **目的:** 海外現場で調べた短いフレーズを、翻訳・保存・復習し、次に自分で言える言葉へ変える
- **初期検証市場:** マカオ・中国語圏でライブポーカーをする日本人（継続利用は未成立）
- **現在の獲得検証:** 海外生活で現地語を繰り返し使い、次は自分で言いたい日本人。現地の教室・講師・生活コミュニティ経由で紹介する
- **本番:** https://phrabit.com
- **開発ポート:** `3010`（`npm run dev`）

---

## 現在のプロダクト定義

Phrabit は、単なる翻訳アプリでも中国語総合教材でもなく、**現場で発生した一言を自分専用の復習資産に変えるプロダクト**。

- コア価値: 翻訳で終わらせず、保存・ドリル・再訪へつなげる
- 中心課題: 翻訳でその場は理解できるが、次の会話で自分の口から出てこない
- コアフロー: 現場で言えない → 翻訳する → 使い方まで理解する → 保存する → ドリルする → 次の現場で使う
- 初期検証: ポーカー現場。ただしプロダクト全体は「ポーカー専用」ではなく「現場フレーズ運用」

---

## 正本資料の優先順位

資料間で迷ったら、用途ごとに優先順位を分ける。

### プロダクト判断

1. `docs/product-vision.md` — Phrabit の現在定義
2. `docs/problem-statements.md` — 解く課題
3. `docs/user-segments.md` — 対象ユーザー
4. `docs/open-questions.md` — 未確定事項・検証すべき問い
5. `docs/roadmap.md` — 現方針に基づく実行計画

`docs/handoff-codex.md` は実装引き継ぎ用の資料。2026-05 時点の狭いポーカー向け表現が残るため、プロダクト定義の判断では `docs/product-vision.md` を優先する。

### 実装・運用判断

1. `AGENTS.md` — Codex 作業時の常設ルール
2. `docs/handoff-codex.md` — 実装の現状・完了事項・見送り事項
3. `docs/glossary.md` — 用語定義
4. `docs/domain-map.md` — DDD/ドメイン構造
5. 対象機能の実装ファイル

古い `README.md` や `SayItNext` 表記の残る資料は歴史的資料として参照し、現方針の正本にはしない。

---

## 作業開始時

1. `docs/product-vision.md` で現在のプロダクト定義を確認
2. `docs/handoff-codex.md` で実装の現状と見送り事項を確認
3. 設計判断を含む場合は `docs/glossary.md`, `docs/domain-map.md`, `docs/open-questions.md` も確認
4. 変更前に関連ファイルを **読んでから** 編集（既存の命名・パターンに合わせる）
5. 実装後は `npm run build` でビルド確認
6. **commit / push / deploy はユーザー明示指示時のみ**

---

## 現フェーズの優先度

- 2026-09-09 承認済み最小リリース：本人が8ファイル（依存更新・長期SRS・共通pinyin/解説整形とテスト）のcommit/push→Preview確認→本番反映を承認。独立候補169テスト/18件無料再生/25ページbuild、Preview `i78jam5mb` の360/390/430pxテスト応答付きUI検証が成功。モデル/プロンプト/予算設定/認証/同期/保存形式は変更しない。直前の本番学習641行を `learning-20260909-005655-owner.dpapi` に暗号化しメモリーDB復元一致、Auth/Storage/端末未同期分は対象外。実験・予算DDL・待機中止UI等の残差分は公開対象外で保持。main pushは本番自動deployのためPreview合格後に行い、公開後はSHA/READY/本番UIを確認する。詳細はhandoff先頭。

- 2026-09-07ロードマップ実行：GeminiはPhrabit専用の前払いへ移行し、本人が￥800を入金、AI Studio上の自動チャージOFFを確認。中国語の既知の还给/袋子/说得と解説内テンプレの読みを追加API呼出なしで限定補正し、`228a6d3`までcommit/push/deploy済み。domain75件・infrastructure/lib53件・対象lint・25ページbuild、本番品質翻訳、解説、例文、直近errorログなしを確認。英中各2紹介先の個別本文と実画面スライドは用意済みだが未送信。営業には送信者名・返信先と宛先/本文の本人承認が必要。最新の残件はroadmap/handoff先頭を参照する。

**海外生活者の継続利用とフリーミアムを検証中（2026-09-05合意）。** 国内レッスン受講生へ主対象を移さない。基本フローは無料で維持し、生成量の追加枠を有料候補として検討する。価格・回数・課金開始日は未決定。

- 2026-09-07合意：翻訳・解説・音声APIは月3,000円。中国・マカオ・香港・台湾などで中国語、英語を生活で使う地域で英語を同優先とする。金額・言語優先を再質問しない。`docs/roadmap.md` に予算設定案・地域差・実機手順・7日判定を準備済み。現状は当月費用/設定未確認で、月額強制停止を設定済みとはしない。普通話/簡体字・米語の現設定と、全地域の品質実証を混同しない。

- やる: 保存・同期・翻訳フローの検証、現地紹介先への提案準備、`ref` 別の入力・保存・復習・自発追加確認、利用原価と有料追加枠の検討
- 無料の基本範囲: 翻訳・ニュアンス調整・保存・ドリル・ログイン同期。基本無料は無制限生成の約束ではない。既存データの閲覧・復習・同期を突然課金で遮断しない
- 外部向けの価格提示・営業送信・提携条件確約は、具体的な内容と宛先の承認後に行う。Xの従来運用への委任を営業メールや提携契約への委任に読み替えない
- やらない: 大規模リファクタ、反応未確認セグメント向け機能、講師/法人機能の先行開発、ドキュメントの勝手な追加
- 迷ったら: `docs/product-vision.md`, `docs/open-questions.md`, `docs/product-iteration-priority.md`, `docs/handoff-codex.md` に従う

---

## コード規約

- **最小 diff。** 依頼された問題だけ直す
- **過剰抽象化禁止。** 1〜2行ヘルパーよりインライン
- **既存コンポーネントを拡張。** 似た機能の再実装禁止
- コメントは非自明なビジネスロジックのみ
- テストは依頼時または意味のあるカバレッジがある場合のみ

---

## 翻訳・会話の設計（変更時は必ず理解）

### 2モード（2026-09-07 本番反映済み）

ユーザーが速度廃止とGPTを含む保護付きPreview比較を承認。翻訳/会話は通常と品質の2択、会話初期値は通常、旧speed入力/復元値は通常へ移す。Azureは障害時fallbackとして維持。Gemini3.5/3.8/GPT-5.6 Lunaを同一20例・実API60生成して追加比較し、結果は `docs/translation-provider-evaluation.md` 先頭参照。品質関連の実行モデルは3.5Flash-Liteへ統一し、`c457ed6` のcommit/pushとProduction `dpl_2ksFARBQmyLvM5XhUP2xgquCYzEf` の公開が完了。本番の保存なし1件でHTTP200・model=gemini-3.5-flash-liteを確認。キー・DBの変更なし。モデル比較用ビルド設定はPreview限定で、通常ビルドに有料評価を組み込まない。現在の事業実行順序は `docs/roadmap.md` 先頭を優先し、以下の未公開表記は作業時点の履歴として扱う。

- `normal` → DeepL → 失敗時 Azure
- `quality` → Gemini（JSON で翻訳+解説+ピンイン）

### 共通の学習言語（2026-09-06 ローカル実装）

- 翻訳・ドリル・保存・会話は `use-learning-language.ts` の共通設定を使う。メニューや各画面での切り替えは4画面へ反映し、画面別の初期言語を作らない。
- `learning-language-storage.ts` がブラウザ内で永続化する。未設定は中国語。アカウントを介した端末間の設定同期は未対応。保存画面の「全て」は一時的な表示フィルター。

### 会話画面の保存方針

- 翻訳 API: `persist: false` → **サーバー・localStorage に保存しない**
- ドリル追加時: 端末のフレーズとSRSを先に保存する。解説は同じIDへ後から補完し、生成失敗でも練習を止めない。
- **会話中にライブラリへ自動保存しない**（2026-05 確定）
- 2026-09-06ローカル改修では、未選択の会話を翻訳履歴へ永続保存しない。明示追加した会話のみ履歴と関連付ける。既存の保存済みデータは削除しない。

### 保存・練習・同期（2026-09-06 ローカル実装）

- 2026-09-07の紹介前チェック：案内を明示追加/ゲスト保存/同期/一時保持に合わせ、長いドリルURLでrefが切れる不具合と同期失敗を追加失敗扱いする計測をローカル修正。実クライアント→API→一時DB・画面/同期回帰・ビルド成功。実API10件では主訳に今回明確な意味逆転は見つからなかったが、返却の还をháiとする解説ピンインの誤りを確認し未修正。問い合わせAPIはテスト送信成功、受信側未確認。実スマホ同期/通信断、対象国と月額予算の回答、公開後の実計測が残る。今回commit/push/deploy/DB変更なし、本番5677d98を維持。詳細・実機手順・全生成文の所在はhandoffの9月7日節を参照。
- 計測修正の公開完了（2026-09-06 23:47 JST）：5ファイルを `5677d98` にcommit/pushしlocal HEADとorigin/main一致。Production `dpl_Ce7sCubB6mh1VWdzQE4ZaUYWovz8` がREADY、phrabit.comも同SHA/IDを確認。確認時の同Deploymentのerror/warningログなし（実利用の新4イベント発生を確認済みとしない）。学習機能のコード変更なし。SQL適用は下記のとおり先行完了。公開後のこの記録はローカル追記。
- 計測修復の承認・適用（2026-09-06）：「よい」を受け、4イベント追加の同一SQLをPreview→本番へ適用済み。本番migration version `20260906144252` にローカル名を揃えた。両DBでservice_roleの4項目INSERT、未知イベント拒否を確認しROLLBACK、テスト行は0。本番の既存914件、RLS/ACL/policyは維持。学習行/Auth設定/UI/APIは変更なし。セキュリティ診断は適用前からの漏洩パスワード保護OFF警告のみ。既存マーケティング差分を除外し、今回のSQL/テスト/引継ぎだけcommit/pushする承認あり。以下の承認待ちは履歴。
- 次作業の準備（2026-09-06）：利用計測の4イベントが本番の旧CHECK制約で拒否されることを読取で再確認し、CLI生成 `20260906142656_expand_product_analytics_translation_events.sql` と新規DB用schemaをローカルで修正した。PGliteテストで旧拒否の再現、全12項目受理、既存行/権限/RLS維持、不正項目とクライアント直接アクセス拒否、再適用を確認。アプリ本体と学習テーブルは未変更。本番/Previewへの適用・commit/pushは未実施。具体的な本番変更内容を提示して承認待ち。手順はhandoff先頭参照。
- 2026-09-06 23:15 JST時点の最新公開：同期修正 `5278045` をcommit/pushし、GitHub main一致。Production `dpl_4EdbxKKczdHmuisDftAVbGobYjij` がREADY、phrabit.comの向き先も同IDを確認。PC本番の再読込後に「同期済み」とスマホ追加の2件を表示し、再読込せず30秒間隔の `/api/phrases` GET 200を本番ログで確認した。確認範囲で新Deploymentのerror/warningなし。実スマホは本人に一度再読込して追加→PC反映を確認してもらう。DB変更なし。即時戻し先は `dpl_CbzbCL7VgEdNX2MS4KvQmWeUMFZU`。以下の未公開/承認前は履歴。
- 2026-09-06同期修正版の公開承認を「おけ」で取得。23:06:39 JSTの学習5テーブルを `learning-20260906-autosync-owner.dpapi` に新規暗号化退避し、本人Windowsアカウントでローカル一時Postgres復元・全件一致を確認。同期修正とテスト/引継ぎのみcommit/push/deployする。DBスキーマ・RLS・計測4項目の許可リストは変更しない。最新公開結果はhandoffの先頭を参照。
- 最新ローカル修正（本番未反映）：スマホ追加が待ってもPCへ出ない報告を受け、表示中/オンライン/ログイン中に30秒ごとに最新セッションで再取得する処理を追加。未同期書込でエラーが出ても取得済みの別端末データを取り込み、失敗した追加/編集/削除はチェックポイントに残して再試行する。旧共通starter IDは、本人クラウドと直前同期状態にないものだけアカウント別の決定的UUIDへ移す。既存の本人データIDや既知の削除を別IDで復活させず、原文/解説/回答履歴を保持する。DB/RLS変更なし。詳細とテスト範囲はhandoffの最新記録を参照。commit/push/deployは追加承認後。
- 最新公開結果：同期修正 `a11e6dc` をpushし、Production `dpl_CbzbCL7VgEdNX2MS4KvQmWeUMFZU` を本番へ反映した。phrabit.comの実ログイン・既存一覧の同期済み・翻訳/ニュアンス調整/明示追加/専用ドリルGood回答を確認。確認用1件の保存と新旧SRSの回答日時一致を本番DBで照合済み。再生成後の再生チェックは未選択。実スマホの再確認は本人に依頼する。初回401の原因は旧IDか認証期限か本番ログだけでは断定できない。利用計測4イベントのDB制約未更新を発見し、学習機能とは別に追加承認待ち。以下の公開前/保留記録は履歴。
- 2026-09-06公開試行：2a62bd7をcommit/pushしProductionビルドは成功したが、既存ログイン端末のPATCH `/api/phrases` に401/未同期を確認し、公開先を17c333dの旧Deploymentへ戻した。端末の旧starter IDを正規化せず同期する不具合を別途再現し、既存のフレーズ/SRSローダー経由へ修正、ID形式不正は認証エラーではなく400に区別した。旧IDとレビュー履歴の退避・既存クラウドとの重複なし・再試行を回帰テスト済み。本番での原因一致/解消は再公開後の確認が必要。戻し先・最新の公開判定はhandoffを参照する。
- 2026-09-06本番反映をユーザーが承認した。直前の学習5テーブルを `learning-20260906-prerelease-owner.dpapi` へ新規暗号化退避し、本人のWindowsアカウントでローカルPostgres復元・全件一致を確認済み。取得21:37:22 JST。Auth/Storage/未同期端末は含まない。アプリ関連差分のみをcommit/pushしてProduction環境変数で公開する。Previewの昇格は禁止。公開結果と戻し先はhandoffの最新記録を参照する。
- 未追加の直前訳・入力・ニュアンスは、同一タブのメモリでアカウント別に保持する。ライブラリ保存とは別であり、再読み込みやタブ終了後の復元は保証しない。
- 翻訳結果上部の「日本語→中国語 / 未追加 / Gemini品質」などのメタ情報バッジは削除し、承認後にPreviewへ反映した。「調整」「再生」は右寄せを維持し、言語選択・翻訳モード切替・保存後の案内や未同期警告は残す。API/保存処理は変更なし。本番未反映。
- ニュアンス入力は、再生の横の「調整」から開くモーダルへ移動した。閉じても補足文を保持し、成功時は自動で閉じ、失敗時は入力を残して再試行する。追加済みフレーズの調整可否・保存処理は変更しない。2026-09-06承認後、Previewへ反映して実APIで開閉・入力保持・再生成成功を確認。本番未反映。
- 翻訳の「使い方・想定返答」は初期展開する。翻訳画面の定型コピーと「同期済み」は省略するが、未同期・失敗・競合の通知は維持する。ニュアンス再生成後の自動読み上げはユーザーが撤回し、従来の手動再生のみに戻した。最新Previewは `poker-chinese-trainer-8hph9spq2-renponpons-projects.vercel.app`（モーダル化・自動読み上げ撤回・メタ情報非表示）。実APIの中国語訳と表示/モーダル開閉を確認。承認後に新URLのGoogle生成元/リダイレクトを追加し、設定再読込で既存8件/9件の維持と追加後9件/10件を照合。本人のログイン・中国語ライブラリ2件の読み込みと「同期済み」を最新Previewでも確認済み。本番アプリ・GitHub未変更。スマホ回答同期・通信断復帰は未確認。
- 新規利用者のライブラリは空。サンプルは任意の練習モードで、本人の保存データ・SRS・回答実績に混ぜない。既存利用者のサンプルや学習履歴は維持する。
- `/drill?phrases=...` は指定した保存済みフレーズを練習する。通常ドリルと同じSRSへ回答を記録する。同期・解説更新で進捗やBadの再出題をリセットしない。
- `account-cache-storage.ts` にアカウント別の直前同期状態と未同期の端末状態を保持し、`reconcile-account-phrase-data.ts` で差分を再試行する。アカウント変更後に古い応答を書き戻さない。
- 別端末で削除された既知のIDへの未同期編集は端末に保持して通知する。自動復活させない。既存IDのPATCHは `existingOnly: true` で更新し、対象が消えていれば409。初回の旧キャッシュ統合とサーバー側の同時書き込み競合は完全な削除履歴管理ではない。
- `save-pack` は保存処理を待って応答し、失敗時は成功扱いにしない。署名付きGETの認証失敗は空一覧ではなく401。
- ローカルテストは固定API・疑似クラウドで実施。本番反映前には実アカウントの複数端末同期検証が必要。
- 2026-09-06最終確認でユーザーがスマホ追加/回答確認を「OK」と報告。Codexでは最新Previewの「同期済み」と既存中国語2件を確認したが、新たな回答のID/時刻照合はしていない。通信断はfetch失敗を使う疑似クラウドで新規追加/回答の保持・再試行・重複なしまで検証済み。実スマホの機内モード検証とは区別する。97件の自動テスト、同期/保存API、端末退避・共通言語・翻訳・個人フレーズ/会話/通常ドリルのブラウザテスト、ビルド成功。既存暗号化バックアップのローカル復元全件一致を再確認した。本番反映の追加承認待ち。Previewの昇格はせず、本番環境変数で再ビルドする。
- 2026-09-06追加の `DeviceSafetyGate` は更新前の端末データを初期同期・通常画面表示より先に退避する。退避失敗時は停止する。`/data-safety` からの復元は現状も別退避し、復元後は同期停止を維持する。復元マーカーや退避データを自動削除して通常利用へ戻さない。
- 2026-09-06追加承認後、学習5テーブルを実Windows利用者renre向けに暗号化退避し、ローカル一時Postgresで全件復元・内容一致を確認済み。検証Supabase `ozhmqkcajgumifthntne` のRLSとDB接続を整備し、承認された既存翻訳/音声キーを値の変更なしでPreviewにも適用した。独立Preview公開・実API/ゲスト保存ドリル確認済み。本番データは検証DBへコピーしない。Google許可URL追加・検証DBのGoogle有効化・本人確認後の実ログインが完了し、英中2件の保存とドリル回答を実DBで照合した。本人のスマホで既存フレーズの表示を確認し、スマホで追加した「お水をください」が検証DBとPCにも反映した。スマホの回答同期・通信断復帰・実マイクは未完了。本番アプリ・GitHubは未反映。再開手順と復元検証の範囲は handoff の「承認後の進捗」を参照する。

### 解説

- 速度・通常: 翻訳時はピンイン・解説空 → ドリル追加時に explain API
- explain API は利用上限から除外
- 解説プロンプト: `src/lib/explanation-prompt.ts`（対訳行の繰り返し禁止）

---

## 秘密情報

- **`.env.local` を commit しない**
- キー類: GEMINI, DEEPL, AZURE, SUPABASE, NOTION, OPENAI 等
- 本番 env は Vercel Dashboard で管理

---

## Git / デプロイ

- ブランチ: 通常 `main`
- リモート: `origin` → GitHub `renponpon/poker-chinese-trainer`
- push → Vercel 自動デプロイ
- force push to main 禁止（ユーザー明示時以外）
- commit メッセージ: 英語、1〜2文、why 重視

---

## ユーザーとのコミュニケーション

- **日本語**で応答
- コード引用は `startLine:endLine:filepath` 形式
- 過度な bold / 箇条書きの羅列を避け、読みやすい文章で
- タスク完了後に不要な follow-up 提案を毎回しない

---

## 触る前に読むべきファイル（タスク別）

| タスク | ファイル |
|--------|----------|
| プロダクト定義 | `docs/product-vision.md`, `docs/open-questions.md` |
| 用語・ドメイン設計 | `docs/glossary.md`, `docs/domain-map.md` |
| ユーザー・課題・ロードマップ | `docs/user-segments.md`, `docs/problem-statements.md`, `docs/roadmap.md` |
| 翻訳・モード | `generation-mode.ts`, `api/phrase/add/route.ts` |
| 会話 | `conversation/page.tsx` |
| 解説 | `api/phrase/explain/route.ts`, `explanation-prompt.ts` |
| ドリル/SRS | `srs.ts`, `drill/DrillRunner.tsx` |
| 保存 | `library/LibraryView.tsx`, `local-phrases.ts` |
| 認証 | `supabase.ts`, `AuthButton.tsx`, `AuthSessionKeeper.tsx` |

---

## 既知の落とし穴

- DeepL「おはよう」→「早上好 早上好」は **API 由来。放置**
- `save-pack` は最大10件/リクエスト（会話ドリル追加はクライアントで chunk）
- BottomNav に会話タブなし（翻訳画面から `/conversation` へ）
- `PHRASE_UPDATED_EVENT` は DrillRunner と Library が購読し、保存後の解説補完を表示へ反映する。
- README の記述は古い部分あり。`handoff-codex.md` を正とする
- `SayItNext` や `Poker Chinese Trainer` 表記は旧名/旧定義。現在は Phrabit として読み替える
- 「ポーカー専用」と断定しない。初期ビーチヘッドはポーカーだが、プロダクト定義は現場フレーズ運用

---

## ドキュメント

- ユーザー依頼なしに `docs/*.md` を新規作成しない
- 実地ログ追記は `docs/macao-field-test-log.md` のみ OK（ユーザー依頼時）
- 設計整理ドキュメントを更新する場合は、関連する正本資料同士の整合を確認する

---

*このファイルと `docs/handoff-codex.md` をセットで保守する。大きな設計変更後は handoff を更新すること。*
